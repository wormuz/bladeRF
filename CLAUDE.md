# bladeRF gateware — інструкції проєкту

FPGA-гейтвер bladeRF 2.0 micro (Cyclone V `5CEBA4F23C8`, швидкісна група
C8 — найповільніша). Quartus 25.1 Standard через `~/soft/q25`.
Симуляція — GHDL. Прошивка FX3 **незмінна** (немає SDK Cypress), тому
частина дефектів обходиться з боку FPGA й хоста.

## ⛔⛔ ПЕРШЕ ПРАВИЛО: НА КОЖЕН ЗНАЙДЕНИЙ ДЕФЕКТ — ГВАРД

Пряма вказівка власника 2026-09-10, після того як один і той самий клас
помилки з'їв кілька днів збірок.

Знайшов дефект — **у тому ж ході**:

1. виправив;
2. додав правило в `hdl/quartus/qcheck` (або хук у `.claude/hooks/`);
3. довів гвард ОБОМА напрямками — він мусить спрацювати на версії
   ДО виправлення й мовчати на версії ПІСЛЯ;
4. записав сюди рядок, чого це коштувало.

⛔ Гвард, який не спіймав би реального дефекту з історії git, — марний.
Перевіряти на самому коміті (`git worktree add /var/tmp/x <sha>~1`), не
на вигаданому прикладі. Обидва мої перші хуки провалили цю перевірку й
були переписані.

⛔ Гвард, що кричить даремно, гірший за відсутній: він вчить себе
ігнорувати. Двічі довелось уточнювати правило після хибного
спрацювання (`rx_epoch_current` названий `rx_*`, але обчислюваний у
системному домені; два різні процеси, зіставлені як один блок).

    ./hdl/quartus/qcheck          перевірити дерево, секунда
    ./hdl/quartus/qcheck --list   що перевіряється і чого коштувало

⛔ НЕ запускати збірку, поки `qcheck` не чистий. Збірка триває
чотирнадцять хвилин; перевірка — секунду.

## ⛔ Збірка «успішна» НЕ означає, що обмеження застосовані

Найдорожчий урок сесії. Помилка під час `read_sdc` відкидає ВСІ
обмеження після точки збою, а фітер продовжує: видає `.rbf`, пише
зведення таймінгу і навіть може збрехати «Can't fit design» при ALM 55%
і M10K 90%, бо працює без обмежень.

    ./hdl/quartus/qgate <лог>     ворота: помилки, критичні, Ignored,
                                  порожні колекції, No path is found,
                                  недійсні винятки, неконстрейнені шляхи,
                                  ПОМИЛКА read_sdc, втрачені межі

⛔ Будь-яка помилка в `read_sdc` = збірка недійсна. Не оцінювати WNS, не
лишати `.rbf`, не запускати свип сідів.

⛔ Закритий таймінг і прив'язані обмеження — РІЗНІ твердження. Нуль
від'ємних записів при відкинутих межах перекосу нічого не доводить.

## ⛔ Обмеження перетинів — попарно НА ІНСТАНС

Шаблон із зірками на обох кінцях збирає регістри КІЛЬКОХ інстансів, і
обмеження просить шлях, якого не існує. Колекції при цьому непорожні,
тому охорона за розміром пропускає; Quartus каже це лише через 332182.

Скільки інстансів насправді:

    time_tamer   2    rx_tamer, tx_tamer
    handshake    5    U_current×2, U_snap×2, U_handshake_timestamp
    dcfifo      10    змішаної ширини + звичайні (loopback, Wishbone×2)

⛔ Три ознаки провалу прив'язки, і жодна не заміняє інших: `Ignored`,
`contains zero elements`, `No path is found`. Третя спрацьовує саме
тоді, коли перші дві мовчать.

⛔ Кожен блок мусить рахувати прив'язані інстанси; нуль — гучно.

⛔ Вендорських файлів обмежень FIFO **два**: `dcfifo.sdc` і
`dcfifo_mixed_widths.sdc`, з різними наборами процедур. Підключати обидва.

⛔ `regexp` по іменах ієрархії НЕ працює: вони містять зворотні слеші від
VHDL-міток (`dcfifo:\fifo_gen:U_dcfifo`), а зворотний слеш у Tcl-regexp
це екранування. Розбивати за роздільником.

## ⛔⛔ ПЕРЕД обмеженням перетину — ПЕРЕВІР, ЧИ ШЛЯХ ІСНУЄ

Найдорожча помилка сесії: три раунди правок шаблону для перетину, якому
нічого не було потрібно.

Мегафункція `dcfifo` ставить власні `set_false_path` на свої покажчики —
двадцять штук, по два на інстанс, зсередини згенерованого IP:

    Info (332165): Entity dcfifo_mu92
      Info (332166): set_false_path -from *rdptr_g* -to *ws_dgrp|...*

Шляхи вирізані ДО того, як наш код виконується, тому будь-який
`set_max_skew` поверх них чесно каже «No path is found». Це читається як
зламаний шаблон, і я тричі «лагодив» його: двокрапка замість риски,
попарність на інстанс, порядок `rdptr_g`/`rdptr_g1p`. Щоразу шаблон
ставав правильнішим — для перетину, який не потребує нічого.

Питання, з якого треба починати, одне і коштує однієї команди:

    get_timing_paths -from <src> -to <dst> -npaths 3 -setup

Нуль шляхів — обмеження не потрібне. Додатково `get_fanins <dst> -synch`
і перевірка `set_max_skew` з ЯВНИМ числом: якщо число зв'язується, а
похідне від періоду ні — проблема в періоді, а не в парі.

⛔ Ловить `qcheck` правилом `sdc-constrains-vendor-cut-path`: сам виклик
`apply_sdc_*dcfifo_for_ptrs` є ознакою, бо його призначення — обмежувати
те, що IP уже вирізав.

## ⛔ Bundled-data перетин: НЕ false path

`set_false_path` забирає з собою й межу перекосу — наступний
`set_max_skew` дістає «No path is found» і відкидається. Шина лишається
без часової вимоги І без обмеження на рознесення бітів, що для утриманих
даних найгірше.

    set_max_delay 100  -from $src -to $dst
    set_min_delay -100 -from $src -to $dst
    set_max_skew  -from $src -to $dst ...
    set_net_delay -from $src -to $dst -max ...

⛔ ОБИДВА кінці завжди. `-from`-only форма переїхала за перетин і
перебила мультицикли SPI/I²C: виміряно вісім порушень до −14.061 нс
на доменах, не пов'язаних із перетином.

⛔ `-to` мусить бути РЕГІСТРОМ. Порт, дріт чи псевдонім дають
«No destination clock period», яке легко прочитати як «обмеження не
потрібне». `handshake.vhd` не має власного регістра захвату
(`dest_data <= source_holding`), тому перший регістр приймача — у
СПОЖИВАЧА, і в кожного свій.

## ⛔ Міждоменні сигнали у статусному слові

Сигнал з `rx_clock`/`tx_clock` не потрапляє в слово, яке читає системний
домен, без синхронізатора. Як VHDL це читається бездоганно — саме тому
30 бітів із 32 пройшли рецензування й знайшлись лише як провалений
шлях setup (виміряно, `hostedxA4-2026-09-10_01.49.45`, Slow 1100mV 85C).

⛔ АБО двох доменів (`tx_flag or rx_flag`) не має домену-власника
взагалі, тож синхронізатор до нього приставити неможливо. Або
синхронізувати кожен окремо й АБО-кати вже в приймачі, або прибрати біт.

## ⛔ FSM: один пріоритетний ланцюг, не кілька `if` поспіль

Два послідовні перекриття після `case` додають по мультиплексору на
логіку наступного стану, і **останнє тихо перемагає**. Коштувало
запасу setup (виміряно, `hostedxA4-2026-09-10_04.09.16`) і того, що
зняття `enable` виводило автомат зі стану
`ABORTED` без нової епохи. Писати `elsif`.

## ⛔ Коментар у C-заголовку — це теж C

`*/` усередині тексту (`U_sync_tx_*/U_sync_rx_*`) закриває блоковий
коментар, і решта речення стає кодом. Зупинило компіляцію `libad936x`,
збірка навіть не дійшла до аналізу таймінгу.

⛔ Заголовок після правки — компілювати, а не перечитувати.

## Швидкий старт

    cd ~/projects/bladerf/hdl/quartus
    ./qcheck                                    # секунда, перед усім
    ~/soft/q25 ./build_bladerf.sh -b bladeRF-micro -r hosted -s A4 -l full
    ./qgate /var/tmp/<лог>

⛔ `pgrep -f quartus` ловить власний рядок команди й показує «зайнято»
на вільній машині. Писати `pgrep -f "quartus_[a-z]"`.

⛔ Проєкт для запитів STA живе в
`hdl/quartus/work/bladerf-micro-A4-hosted` (проєкт `bladerf`, ревізія
`hosted`), не в теці знімка. `create_timing_netlist -post_fit` несумісне
з явними умовами кута — вибирати щось одне.

## Залізо

    bladeRF 2.0 micro, серійник f695006ba84a40daa7b777c6a6eba782
    AD9361 Rev 2, SuperSpeed, FPGA 0.16.0 (вендорська, з флеша)

⛔ Прошивати ТІЛЬКИ за прямою командою власника: «все ще сире».

⛔ `LD_PRELOAD` на `libbladeRF.so.2` обов'язковий для власної збірки;
`LD_LIBRARY_PATH` не працює (розширення не має залежності на бібліотеку).

## Деталі

Робочі документи — OpenContext, тека `bladerf`:
`gateware-build.md` (стан, таймінг, пастки),
`fx3-workarounds-in-gateware` (обхід дефектів прошивки FX3).
